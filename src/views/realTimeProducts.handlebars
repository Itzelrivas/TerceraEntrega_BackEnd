<h1 style="background-color: beige; padding: 2%">Todos los productos:</h1>
<h4 style="background-color: rgb(11, 119, 117); padding: .5%; color: white">Actualizados en tiempo real...</h4>
<div id="productContainer">
    {{#each products}}
    <div class="containerRealTime">
        <h2 style="color: beige">{{this.title}}</h2>
        <h3>Descripción: {{this.description}}</h3>
        <h3>Código: {{this.code}}</h3>
        <h3>Categoría: {{this.category}}</h3>
        <h3>Piezas disponibles: {{this.stock}}</h3>
        <h3>Precio: ${{this.price}}</h3>
    
        {{#each this.thumbnail}}
        <img style="width: 20%;" src="/img/{{this}}" alt="{{../title}}"></img>
        {{/each}}
        
    </div>
    {{/each}}
</div>

<script src="/socket.io/socket.io.js"></script>
<script src="/js/realTimeProducts.js"></script> 